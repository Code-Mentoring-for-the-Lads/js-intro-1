// greet.js

// Task: Write a function called greet that takes a string as input and returns the string with 'Hello, ' appended to the beginning.
// Example: greet('Lads') should return 'Hello, Lads'

function greet(name) {
    // Your code here
}

module.exports = greet;
// to test: run `npm run test greet`